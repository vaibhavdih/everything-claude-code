{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "Auto-format Python files in Frappe projects",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "condition": "PROJECT_TYPE=frappe",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/hooks/frappe-format.js\"",
            "description": "Auto-format Python files with Black/Ruff (Frappe projects)"
          }
        ],
        "description": "Format Python files after edits in Frappe projects"
      }
    ]
  }
}
